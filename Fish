import java.util.*;

class Solution {
    public int solution(int[] A, int[] B) {
        // write your code in Java SE 8
        Stack<Integer> liveFish = new Stack<Integer>();
        for(int i = 0; i < A.length; i++)
        {
            if(liveFish.peek() != null && )
        }


    }
}